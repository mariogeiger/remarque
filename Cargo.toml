[workspace]
members = [
    "app/core",
    "app/tablet",
    "reverse-engineering/native-observer",
    "reverse-engineering/native-replay",
]
resolver = "3"

[profile.release]
lto = true
opt-level = "s"
panic = "abort"
strip = true
